
import {
    StyleSheet,
} from 'react-native';

export default Styles = StyleSheet.create({
    containerStyle: {
        justifyContent: 'center',
        flex: 1,
        marginTop: 20,
    },
    row: {
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'flex-start',
    },
});